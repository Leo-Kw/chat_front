// global-state
export namespace GlobalState {
  export interface UserInfoType {
    id: number
    sex: number
    name: string
    email: string
    sign: string
    role: number
    room_id: string
    avatar: string
    account: string
  }
}
